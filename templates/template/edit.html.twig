{% extends 'base.html.twig' %}

{% block title %}Edit Template{% endblock %}

{% block body %}
    <div class="col-12 col-sm-10 mx-auto my-5 pt-5 ">
        <h1>Edit Template</h1>

        {{ include('template/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_template_index') }}">back to list</a>

        {{ include('template/_delete_form.html.twig') }}
    </div>
{% endblock %}
